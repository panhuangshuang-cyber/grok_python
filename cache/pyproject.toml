[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "grok-cache-pkg"
version = "0.1.1"

[tool.setuptools]
py-modules = ["constant_var", "data_setup","constant_num"]